/**
 * Demo-shortened cooling-period duration. A production deployment would use
 * a much longer window (hours); this is compressed so the mandatory pause
 * is demonstrable live.
 */
export const COOLING_PERIOD_MS = 45_000;
export const MAX_INTENT_QUESTIONS = 3;
